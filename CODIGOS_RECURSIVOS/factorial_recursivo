#Factorial recursivo en Python.
#By: Jorge Ulises Zapata Arteaga.
#Analisis y Diseño de Algoritmos 3CM2.
n=int(input('Ingrese el número a sacar el factorial: '))
def facto(n):
    if(n<=1):
        return 1
    else:
        return n*facto(n-1)

muestra_factorial=facto(n)
print(muestra_factorial)
